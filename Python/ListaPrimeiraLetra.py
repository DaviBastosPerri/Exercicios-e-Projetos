# Use Compreensão em listas para criar uma lista das primeiras letras de cada palavra na string abaixo:

st = 'Create a list of the first letters of every word in this string'
s = st.split()
print([l[0] for l in s])
l = 1
